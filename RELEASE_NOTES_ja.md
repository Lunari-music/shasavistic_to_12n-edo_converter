# リリースノート

---

## [v1.0.1] - 2026-09-03

### 変更の概要
- **オフセット対応と設定の保存:** 基準音および主音への $n$-EDO ステップオフセット設定に対応し、`localStorage` による設定保存とリセット時の自動復元を実装しました。
- **基準ピッチの標準化:** 音名+ステップ表示の判定基準を絶対的な A=440 Hz ピッチに統一しました。
- **オフセット表記の正規化:** マイナス表記のオフセットを、1つ低い音名とプラス表記のオフセットに自動変換する処理を追加しました（例: `[A# - <sup>48</sup>2']` → `[A + <sup>48</sup>2']`）。
- **バグ修正:** 主音オフセット設定時の音名誤表示、`tonic_step_12` の二重加算、主音非表示時の相対ステップ参照点の不具合、負のオフセット表示エラー、オクターブ正規化の不整合を修正しました。

### 🌟 機能追加 (Added)
- 基準音（Base Pitch Note）への $n$-EDO ステップオフセット指定機能を追加。
- 主音（Tonic）への $n$-EDO ステップオフセット指定機能を追加。
- ページリセットおよび `localStorage` 保存時に両オフセット設定を永続化。

### 🔄 仕様変更 (Changed)
- 絶対音名判定の処理を主音オフセット計算から分離。
- 音名+ステップ表示の音名判定基準を絶対 A=440 Hz ピッチに変更。
- オフセット表示の自動正規化を追加し、1つ低い音名＋プラスオフセット表記を優先（例: `[A# - <sup>48</sup>2']` → `[A + <sup>48</sup>2']`）。

### 🐛 バグ修正 (Fixed)
- 「音名+ステップ表示」OFF時、相対的な $n$-EDO ステップ計算の参照点に主音周波数が正しく使用されるよう修正。
- 主音が `A + <sup>48</sup>4'` の設定時に、440 Hz が `[G#]` と判定される不具合を修正。
- `calc_nedo_step_and_cents()` 内で `tonic_step_12` が二重に加算され、A以外の主音設定時に音名がずれて表示される（例: `[C]` であるべき場所が `[D#]` になる）不具合を修正。
- 負の折り返しオフセットが誤った音名に対して表示される問題を修正。
- 内部基準ピッチと主音参照の間のオクターブ正規化処理の一貫性を確保。

---

## [v1.0.0] - 初回リリース

Shasavistic Interval to $12n$-EDO Converter の初回正式リリース。

### 🎵 主な機能
- **Shasavistic理論に基づく音程計算:** Shasavistic理論に従い、多次元音程ベクトルのピッチと周波数を計算。
- **$12n$-EDO 音名マッピング:** 計算された周波数を $12n$-EDO ステップにマッピングし、12音名判定とセント差を表示。
- **インタラクティブなUIとカスタム設定:**
  - 多次元音程カウントの動的な入力コントロール。
  - 基準ピッチ（A=440 Hz標準）、基準音、平均律（$n$-EDO）の柔軟な設定。
  - ベクトル移動を可視化するSVGビジュアル表示。
- **音声合成:** Web Audio API を使用し、基準音と計算された音程周波数を即座に試聴可能。
- **ローカル保存:** ブラウザの `localStorage` を使用してカスタム設定を保存・再読み込み。
